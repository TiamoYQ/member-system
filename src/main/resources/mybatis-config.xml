<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 别名配置（可选） -->
    <typeAliases>
        <package name="com.ecommerce.entity"/> <!-- 扫描实体类包 -->
    </typeAliases>

    <!-- 数据源环境配置 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/> <!-- 数据库驱动 -->
                <property name="url" value="jdbc:mysql://localhost:3306/your_db?useSSL=false"/> <!-- 数据库URL -->
                <property name="username" value="root"/> <!-- 用户名 -->
                <property name="password" value="123456"/> <!-- 密码 -->
            </dataSource>
        </environment>
    </environments>

    <!-- 映射器配置（扫描Mapper接口） -->
    <mappers>
        <package name="com.ecommerce.mapper"/> <!-- Mapper接口所在包 -->
    </mappers>
</configuration>